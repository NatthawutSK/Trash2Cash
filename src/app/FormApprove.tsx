import { View, Text } from "react-native";
import React from "react";

type Props = {};

const FormApprove = (props: Props) => {
	return (
		<View>
			<Text>FormApprove</Text>
		</View>
	);
};

export default FormApprove;
