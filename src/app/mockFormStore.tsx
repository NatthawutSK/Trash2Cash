import { View, Text } from "react-native";
import React from "react";
import FormStore from "@/components/auth/FormStore";

type Props = {};

const mockFormStore = (props: Props) => {
  return <FormStore />;
};

export default mockFormStore;
