import { View, Text } from "react-native";
import React from "react";

type Props = {};

const TestSignout = (props: Props) => {
  return (
    <View>
      <Text>testSignout</Text>
    </View>
  );
};

export default TestSignout;
