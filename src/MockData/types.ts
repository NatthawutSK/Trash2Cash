export type TypeStore = {
  name: string;
  address: string;
  phone: string;
  line: string;
};

export type TypeTrashMaterial = {
  name: string;
  img: string;
  recieveAmount: string;
  price: number;
};
