import { View, Text } from "react-native";
import React from "react";

type Props = {};

const editMaterialStore = (props: Props) => {
  return (
    <View>
      <Text>editMaterialStore</Text>
    </View>
  );
};

export default editMaterialStore;
